<Window x:Class="MusicApp.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registro de Usuario" Height="340" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Crea tu cuenta" Grid.Row="0"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="Nombre de Usuario:" Grid.Row="1" Margin="0,0,0,5" FontSize="14"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="2" Padding="8" Margin="0,0,0,15"/>

        <TextBlock Text="Contraseña:" Grid.Row="3" Margin="0,0,0,5" FontSize="14"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Padding="8" Margin="0,0,0,20"/>

        <Button Content="Registrarse" Grid.Row="5"
                Width="150" Height="40"
                HorizontalAlignment="Center"
                Click="RegisterButton_Click"
                FontSize="16" FontWeight="SemiBold"
                Background="#4CAF50" Foreground="White"
                BorderBrush="#388E3C" BorderThickness="1"
                Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#66BB6A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#388E3C"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>